apply plugin: 'java'
apply plugin: 'eclipse'

repositories {
	mavenCentral()
}

dependencies {
	compile jrubyVersion
}

sourceCompatibility = 1.6
version = newBoneOSVersion
jar {
	manifest {
		attributes 'Implementation-Title': 'BoneOS',
				   'Implementation-Version': version
	}
}
